package bigbade.pingwars.upgrades.maxplayers;

import bigbade.pingwars.upgrades.Upgrade;

public class MaxPlayersThree extends Upgrade {
    public MaxPlayersThree(byte id) {
        super(id, "Max Players 3", 20000);
    }
}
